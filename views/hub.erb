<% @posts.order(created_at: :desc).limit(100).each do |post| %>
    <% next if User.find_by(username: post.user.username).active == false%>
    <h2><%= post.user.username %></h2>
    <h3><%= post.content %></h3>
<% end %>